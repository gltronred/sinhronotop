"IDteam";Название;Город;Флаг;"IDplayer";Фамилия;Имя;Отчество
<% @results.each do |result|-%>
<% result.team.plays.find_all{|item|item.event_id == result.event.id}.each do |play|-%>
<% player = Player.find(play.player_id) -%>
<%=h result.team.rating_id %>;<%=h result.team.name %>;<%=h result.team.city.name %>;<%=play.get_rating_status()%>;<%=h play.get_rating_id %>;<%=h player.lastName %>;<%=h player.firstName %>;<%=h player.patronymic %>
<%end-%>
<%end-%>