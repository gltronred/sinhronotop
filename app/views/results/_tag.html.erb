<% unless @event.game.tournament.tags.empty? %>
  <p>
    <%= f.label "Категория" %><br />
    <%= select_tag :tag_id, options_from_collection_for_select(Tag.all, :id, :to_s, tag_id, {:include_blank => true}) %> 
  </p>
<% end %>